# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

Foundation

## Current Goal

Editor home is wired end-to-end to the project API: sidebar lists real owned/shared projects (server-fetched), and create/rename/delete hit the real endpoints. Next: build out the actual workspace canvas (the `/editor/[projectId]` page is currently a placeholder).

## Completed

- **07-wire-editor-home**: Wired the editor home sidebar + dialogs to the real project API (replaced all mock data). `app/editor/layout.tsx` (server component) resolves the user via new `lib/auth.ts` `getCurrentUserContext()` (Clerk `currentUser()` → `{ userId, primary email }`), fetches owned (`listProjectsForOwner`) + shared (`listSharedProjectsForCollaborator(email, userId)`, excludes own projects) in parallel, maps DB records → UI `Project` (slug via `slugify`), and passes both lists to `EditorShell` → `ProjectSidebar`. No client-side fetch on initial load (`/editor` is now `ƒ` dynamic). Hook renamed/rebuilt: `hooks/use-project-dialogs.ts` → `hooks/use-project-actions.ts` (`useProjectActions`) — owns dialog state **and** real mutations: **Create** generates a stable short suffix on open, builds `roomId = slugify(name)-suffix` (previewed live in the dialog), `POST /api/projects` with `{ id: roomId, name }`, then `router.push(/editor/{project.id})`; **Rename** `PATCH`es then `router.refresh()`; **Delete** `DELETE`s then redirects to `/editor` if the deleted project is the active workspace (`useParams().projectId`) else `router.refresh()`. Errors surfaced via an `error` field rendered in each dialog (`text-error`). **Project id === Liveblocks room id**: to honor "project ID and room ID stay aligned", `createProjectForOwner` + `POST` now accept an optional explicit `id` (cuid default still applies when omitted — spec 06 intact); duplicate id → `409` (P2002 caught). Sidebar list rows are now `next/link` to `/editor/{id}` (closes sidebar on navigate); dropdown actions unchanged. Added placeholder `app/editor/[projectId]/page.tsx` (server) that gates access via new `findAccessibleProject(projectId, userId, email)` (owner OR collaborator) → `notFound()` otherwise, so create-navigation has a real destination. Removed `lib/mock-projects.ts`. `npm run build` + lint pass.

- **06-project-apis**: Backend project REST API per `context/feature-specs/06-project-apis.md` (no UI wiring). `lib/projects.ts` (`server-only`) owns persistence: `listProjectsForOwner`, `createProjectForOwner` (trims name, defaults to `Untitled Project`, description→null, DB-assigned cuid), `getProjectById`, `renameProject`, `deleteProject`. `app/api/projects/route.ts` — `GET` (lists the Clerk user's owned projects, newest first) and `POST` (creates; validates `name`/`description` are strings if present, 400 on bad JSON, returns 201). `app/api/projects/[projectId]/route.ts` — `PATCH` (rename; requires non-empty string `name`) and `DELETE`, both owner-only. All handlers: `await auth()` → 401 if no `userId`; mutations fetch the project (404 if missing) then 403 if `ownerId !== userId`. Uses Next 16 `RouteContext<'/api/projects/[projectId]'>` with `await ctx.params`. No zod (not a dep) — input validated manually at the boundary. Fixed two latent phase-05 `lib/prisma.ts` bugs surfaced by the first app-side Prisma import: (1) import path `@/generated/prisma/client.js` → `client` (Prisma 7 emits `client.ts`, Turbopack couldn't resolve the `.js` extension); (2) the connection-string branch made `prisma` an uncallable union type — pinned `createPrismaClient(): PrismaClient` and bridged the Accelerate-extended branch with `as unknown as PrismaClient` (cacheStrategy args unused). `npm run build` + lint pass.

- **01-design-system**: shadcn/ui initialized (Tailwind v4, base-nova style); Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea added; lucide-react installed; `lib/utils.ts` with `cn()` helper created; `globals.css` rewritten with full dark-only theme — project CSS custom properties (`--bg-base`, `--bg-surface`, `--text-primary`, `--accent-primary`, etc.) defined in `:root` and mapped to Tailwind tokens via `@theme inline`; shadcn semantic vars (`--background`, `--foreground`, etc.) mapped to project dark palette.
- **02-editor**: `components/editor/editor-navbar.tsx` — fixed-height top navbar (h-12), left toggle button using `PanelLeftOpen`/`PanelLeftClose` based on sidebar state, dark bg-surface with bottom border; `components/editor/project-sidebar.tsx` — floating overlay sidebar (z-50, no content push), slides in from left (translate-x transition), accepts `isOpen`/`onClose` props, "Projects" header with close button, shadcn Tabs (My Projects / Shared) with empty placeholder states, full-width "New Project" button with Plus icon at bottom.
- **05-prisma**: Prisma 7.8 + Prisma Postgres live end-to-end, implemented per `context/feature-specs/05-prisma.md`. Deps: `prisma`, `@prisma/client`, `@prisma/adapter-pg`, `pg`, `dotenv`, `@prisma/extension-accelerate` (runtime) + `@types/pg`, `tsx` (dev). `package.json` is `"type": "module"` (safe — all configs are `.ts`/`.mjs`) with `postinstall: prisma generate`. **Multi-file schema**: `prisma/schema.prisma` holds only the `prisma-client` generator (output `../generated/prisma`, gitignored) + `postgresql` datasource; `prisma/models/project.prisma` holds the models. `prisma.config.ts` `schema` points at the `prisma` **directory** (so both `.prisma` files merge), loads `DATABASE_URL` via `dotenv/config`, and wires `migrations: { path, seed: "tsx prisma/seed.ts" }`. Models: `Project` (`ownerId` Clerk ID, `name`, `description?`, `status ProjectStatus @default(DRAFT)` enum DRAFT/ARCHIVED, `canvasJsonPath?`, timestamps, `@@index([ownerId])`, `@@index([createdAt])`) and `ProjectCollaborator` (`email`, cascade-delete relation to Project, `@@unique([projectId, email])`, `@@index([email])`, `@@index([projectId, createdAt])`). `lib/prisma.ts` exports one `globalThis`-cached client (`server-only`) that **branches on `DATABASE_URL`**: `prisma+postgres://` → `new PrismaClient({ accelerateUrl }).$extends(withAccelerate())`; otherwise the `PrismaPg` direct adapter. DB is the linked Prisma Postgres `db_cmpyc58610...` (direct `postgres://` in gitignored `.env`). Old un-specced draft schema (slug/Collaborator-by-userId/Spec/TaskRun) was reset **with explicit user consent** (`PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION`) and replaced by a single clean migration `20260603173836_init`. `prisma/seed.ts` seeds one demo project + collaborator (idempotent via `deleteMany` on `ownerId`); `scripts/verify-prisma.ts` confirms a read. `migrate dev`, `db seed`, `verify-prisma`, and **`npm run build`** all pass.
- **04-project-dialogs**: Editor home screen and project create/rename/delete dialogs, mock data only (no API/persistence). `app/editor/page.tsx` now renders `components/editor/editor-home.tsx` — centered, card-less heading "Create a project or open an existing one" + description + `New Project` button (Plus icon) that opens the Create dialog. `hooks/use-project-dialogs.ts` is the dedicated hook owning dialog kind (`create`/`rename`/`delete`), the active project, the controlled name field, and the `isSubmitting` flag; submit handlers simulate the async round-trip (350ms) then close. `components/editor/project-dialogs-provider.tsx` calls the hook once, exposes `openCreate/openRename/openDelete` via context, and renders the three dialogs at the top of the editor tree — so both the sidebar and the editor home share one dialog instance. `EditorShell` wraps its tree in `ProjectDialogsProvider`. Dialogs (`components/editor/dialogs/*`): Create has a live `slugify()` preview that updates as you type; Rename prefills + auto-focuses/selects the name (Enter submits, current name shown in description); Delete is a destructive-only confirmation with no input. Sidebar (`project-sidebar.tsx`) renders mock owned/shared lists via `project-list-item.tsx`; owned items get a `MoreHorizontal` dropdown (Rename/Delete), actions hidden for shared/collaborator projects. Mobile: outside-tap closes the sidebar and a `bg-black/60 md:bg-transparent` scrim is shown. `slugify()` added to `lib/utils.ts`; `types/project.ts` + `lib/mock-projects.ts` added. `dropdown-menu` shadcn component (base-ui Menu) added. Build + lint pass.
- **03-auth**: Clerk wired into the app. `@clerk/ui` installed; `ClerkProvider` wraps the root layout in `app/layout.tsx` using the `dark` theme from `@clerk/ui/themes`, with appearance `variables` mapped to the app's CSS custom properties (no hardcoded colors). Editor chrome (`EditorShell`) relocated out of the root layout into a new `/editor` segment (`app/editor/layout.tsx` + placeholder `page.tsx`). `app/page.tsx` now redirects: authenticated → `/editor`, unauthenticated → `/sign-in`. Auth pages live under an `(auth)` route group with a shared two-panel layout (left: logo + tagline + text feature list, hidden below `lg`; right: centered Clerk form); `app/(auth)/sign-in/[[...sign-in]]/page.tsx` and `sign-up/[[...sign-up]]/page.tsx` render `<SignIn>`/`<SignUp>`. Route protection via `proxy.ts` at the root (Next 16 replacement for `middleware.ts`, which was deleted) — protected-first: public routes (`/`, sign-in, sign-up) derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars, everything else `auth.protect()`. Clerk's built-in `UserButton` added to the editor navbar right section. `npm run build` passes.

## In Progress

## Next Up

## Open Questions

## Architecture Decisions

- Prisma 7 keeps the connection URL out of `schema.prisma` (datasource has provider only); `DATABASE_URL` is wired through `prisma.config.ts` for the CLI and resolved at runtime in `lib/prisma.ts`, which branches on the URL scheme — Accelerate (`prisma+postgres://`) uses `accelerateUrl` + `withAccelerate()`, everything else uses the `PrismaPg` direct adapter (an Accelerate URL must never be handed to a driver adapter). The generated client is imported from `@/generated/prisma/client.js` (not `@prisma/client`); the dir is gitignored + regenerated on `postinstall`.
- Schema is multi-file: `prisma.config.ts` `schema` points at the `prisma` directory, so `prisma/schema.prisma` (generator + datasource) and `prisma/models/*.prisma` (models) are merged. Note: `prisma migrate dev` did not pick up the regenerated client reliably after the schema swap — run `prisma generate` explicitly if the client looks stale (`P2022` on a dropped column).
- Collaborators are modeled by **email** (`ProjectCollaborator.email`), not Clerk user ID — invites can precede the invitee having an account. `Spec`/`TaskRun` tables from the architecture storage model are deferred to a later phase (not in the 05-prisma spec).
- Project dialog state is owned by one `useProjectDialogs` hook instance, lifted into `ProjectDialogsProvider` (rendered inside `EditorShell`) and shared via context. Both the sidebar and the editor home screen open the same dialogs, which are rendered once at the top of the editor tree rather than duplicated per trigger.
- Editor chrome lives at `/editor`, not the root route. The root layout holds only `ClerkProvider` + fonts; the `EditorShell` (navbar + sidebar) is the layout for the `/editor` segment so auth pages and the `/` redirect render without editor chrome.
- Clerk URL env vars added to `.env.local`: `NEXT_PUBLIC_CLERK_SIGN_IN_URL`, `NEXT_PUBLIC_CLERK_SIGN_UP_URL`, and the two `*_FALLBACK_REDIRECT_URL` vars (both `/editor`). Standard Clerk names — none invented.
- `@clerk/ui` v1.15 appearance schema uses `colorForeground`/`colorPrimaryForeground`/`colorInput`/`colorMuted` naming (not the older `colorText`/`colorInputBackground`).

## Session Notes

- **Post-logout infinite render fix**: logout had no `afterSignOutUrl`, so Clerk's default landing was `/` — a server route that recomputes auth and `redirect()`s. Landing a just-signed-out user there bounced during the sign-out handshake settling, leaving Next's dev "Rendering …" indicator stuck. Fixed by setting `afterSignOutUrl="/sign-in"` on `<ClerkProvider>` (`app/layout.tsx`) so logout lands directly on the public, terminal sign-in page. Verified routing resolves in single 307s (`/`→`/sign-in`, `/editor`→`/sign-in?redirect_url=…`, `/sign-in`→200); build passes.
